# LaunchBoard V9.4 — Mobile Profile Menu

Replace `dashboard.html` and `dashboard-responsive.css`, then add `mobile-profile-menu.js`.

On phones, tapping the profile picture/name opens a bottom sheet with Edit profile, View profile, Saved products, Following, and Log out. Desktop remains unchanged.

After deployment, increase the service-worker cache version and clear site data/PWA cache.
