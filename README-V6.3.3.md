# LaunchBoard v6.3.3 — Inline Search Filter

Changes:
- Removed the separate Filters and Sort buttons below Discover Products.
- Added one compact Filter button inside the main search bar.
- The Filter sheet still contains:
  - Category
  - Product type
  - Sort by
- Search updates automatically while typing.
- Pressing Enter/Search on the keyboard still works.
- Mobile-only change; desktop remains unchanged.

Replace:
- index.html
- app.js
- styles.css

Clear the Android app/WebView cache after uploading.
